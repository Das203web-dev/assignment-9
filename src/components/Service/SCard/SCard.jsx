import React from 'react';

const SCard = ({ data }) => {
    console.log(data)
    const arr = []
    if (data === !undefined) {
        arr.push.data
    }
    console.log(data)
    return (
        <div>

        </div>
    );
};

export default SCard;